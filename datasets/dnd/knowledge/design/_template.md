<!-- Design workflow instruction. Usage: pinned by the design operator to guide a specific build-out task. Contains instructions for the AI on what to ask, what to look up, and what to produce. These are NOT objects the player creates — they are tools for the design operator. -->
Instructions for the design operator on how to approach a specific build-out task.

Each design object describes:
- What the task produces (which KB object types)
- What to ask the user before starting
- What to look up in existing KB
- How to structure the output
- Common pitfalls to avoid
