# Testing dataset — rules.system shadow

When `testing` is listed after `rpg` in `[project] datasets`, this object replaces
the bundled Lasers and Feelings `rules.system` body from the RPG dataset.

E2E marker: TESTING_RULES_SYSTEM_SHADOW
